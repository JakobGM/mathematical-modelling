"""Dummy test to check if tests run properly."""

import numpy as np


def test_dummy():
    """TODO: Delete this as soon as we have a proper test."""
    null_vector = np.array([0, 0, 0])
    assert not np.any(null_vector)
